/*
 * File-level comment before any statement.
 */

/** Adds two numbers while preserving statement JSDoc. */
export function add(left: number, right: number): number {
  return left + right;
}
// trailing EOF comment after the last statement
