# Independent methodology review — B-2 implementation plan v1 (Codex output, archived verbatim)

**Model:** gpt-5.6-sol, reasoning `xhigh`, sandbox read-only, repo-grounded.
**Brief:** `2026-08-01-item-b2-plan-review-brief.md`. **Date:** 2026-08-01.
**Plan reviewed:** `docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md` (v1).
**Tokens used:** 476,218. **Verdict:** RE-GROUND (honored — v2 is a re-grounded rewrite, not a patch).

**In-session source verification (2026-08-01), per repo rule — pivotal claims CONFIRMED:**
- Blocker 1: `full_key_free_acceptance.rs:1943` `assert_exact_object_keys` pins the exact
  five-key validationProfile JSON — required timeout fields on TscOnly would break the
  byte-identical acceptance. v2: timeouts OPTIONAL/omitted on TscOnly, REQUIRED on Behavioral.
- Blocker 3: session.rs advance early-returns any non-Ready state (~703); `expire_leases`
  is invoked only from submit-path lease checks and offer-expiry flows — nothing in the
  advance path re-drives a stranded claim. v2: explicit atomic release-and-requeue contract
  (`Kernel::release_claim_for_retry`) modeled on the lease-expiry optimistic transition.
- Blocker 4: previously self-verified — `request_at_with_size` starts the deadline before
  the worker mutex and spends it on queue+sync+exchange (persistent.rs:227 area).
- Major 5: candidate.ts:181 maps EVERY unexpected mutate-stage exception to `mutationFailed`
  (self-verified earlier). v2: distinct worker `intentRejected` code for known application
  rejections; residual `mutationFailed` becomes OPERATIONAL.
- Answer 5: `export function greet` at `examples/medium/src/users/greet.ts:7`
  (v1's `src/features/greet` path was wrong); the namespace-import sleep lever stands.
- Blocker 2, Majors 6-9, Minor 10: accepted on the review's direct file:line evidence
  (audit-before-bind at session.rs:135-148 and the worker realpath containment at
  candidate.ts:449 were read in-session during grounding).

---

RE-GROUND

## Ranked findings

1. **Blocker — Task 5 contradicts the hard byte-identical tsc-only contract.** The plan promises unchanged no-manifest wire bytes at [plan:38](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:38), but then makes `tscTimeoutMs` and `vitestTimeoutMs` required in both profile variants at [plan:693](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:693). Current acceptance code pins the exact five-field profile at [full_key_free_acceptance.rs:1943](/Users/toddhebebrand/Strata/crates/strata-kernel/tests/full_key_free_acceptance.rs:1943), and the governing spec preserves byte identity at [item-b-design.md:141](/Users/toddhebebrand/Strata/docs/superpowers/specs/2026-07-31-item-b-design.md:141). Keep timeout fields absent for no-manifest tsc-only requests—e.g. optional/omitted for `TscOnly`, required for `Behavioral`—while allowing a manifest-backed tsc-only profile to carry them.

2. **Blocker — direct-child `SIGKILL` does not meet the required process-group cleanup contract.** The spec explicitly requires killing spawned process groups at [item-b-design.md:104](/Users/toddhebebrand/Strata/docs/superpowers/specs/2026-07-31-item-b-design.md:104); the plan substitutes `spawnSync` direct-child killing at [plan:718](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:718). Vitest is 3.2.4 at [pnpm-lock.yaml:1370](/Users/toddhebebrand/Strata/pnpm-lock.yaml:1370), and its installed Tinypool defaults to worker threads at [tinypool/index.js:235](/Users/toddhebebrand/Strata/node_modules/.pnpm/tinypool@1.1.1/node_modules/tinypool/dist/index.js:235), so `--pool=threads` avoids Vitest-owned fork workers. It does not constrain fixture code: the worker only validates fixture path/type at [candidate.ts:445](/Users/toddhebebrand/Strata/packages/kernel-bridge/src/candidate.ts:445), and the verifier copies and executes those tests at [corpusRun.ts:284](/Users/toddhebebrand/Strata/packages/verify/src/corpusRun.ts:284). A fixture can spawn descendants, which survive killing only Vitest. Implement actual process-group supervision; this cannot be accepted merely as a closing-record divergence.

3. **Blocker — operational failures can strand a claim forever.** Task 3 relies on automatic lease re-offer at [plan:451](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:451). Logical ticks advance only when requests are journaled at [session.rs:426](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bin/strata_kernel_service/session.rs:426), and `advance` immediately returns for a claimed change set at [session.rs:703](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bin/strata_kernel_service/session.rs:703). Claim release occurs only when `expire_leases` is explicitly invoked, at [coordinator.rs:660](/Users/toddhebebrand/Strata/crates/strata-kernel/src/coordination/coordinator.rs:660); the advance path does not invoke it. Even repeated advances therefore cannot re-drive the work. The plan needs an explicit atomic requeue/release contract—or cancellation with non-retryable semantics. `cancel_change_set` remains available only to the owning client, enforced at [session.rs:1132](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bin/strata_kernel_service/session.rs:1132).

4. **Blocker — the persistent deadline path does not implement the specified nesting, and transport timeouts are not cleanly classified.** The plan passes the derived candidate budget directly into the router at [plan:735](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:735), but `request_at_with_size` starts that deadline before acquiring the worker mutex and uses it for queue wait, hydration/sync, and the candidate exchange at [persistent.rs:227](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bridge/persistent.rs:227). The existing queue test deliberately pins that behavior at [persistent.rs:1164](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bridge/persistent.rs:1164). Queue/sync time can therefore consume the budget intended to cover materialization, validation, rollback, fingerprinting, and cleanup. Also, worker-reported timeout codes are distinguishable as `MirrorCandidate::Failed`, but all transport failures reach the same generic executor fallback at [executor.rs:145](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bridge/executor.rs:145). Introduce typed transport phases and separate queue allowance from the candidate execution deadline; do not string-match generic errors.

5. **Major — `mutationFailed` is broader than semantic rejection.** The plan classifies every generic mutate-stage exception as semantic at [plan:44](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:44), while the worker maps every unexpected exception during that stage to `mutationFailed` at [candidate.ts:181](/Users/toddhebebrand/Strata/packages/kernel-bridge/src/candidate.ts:181). Target-not-found and invalid target errors are legitimate candidate rejection, as shown at [rename.ts:31](/Users/toddhebebrand/Strata/packages/store/src/rename.ts:31). But the same bucket includes internal payload/callsite invariant failures such as [addParameter.ts:134](/Users/toddhebebrand/Strata/packages/store/src/addParameter.ts:134) and [addParameter.ts:167](/Users/toddhebebrand/Strata/packages/store/src/addParameter.ts:167). Split known intent-application rejections from unexpected mutation/invariant failures before treating the code as semantic.

6. **Major — several tasks are not independently green or commit-complete.**

   - Task 1 proposes an integration test calling `parse_bridge_response`, but that function is `pub(crate)` at [bridge/protocol.rs:1203](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bridge/protocol.rs:1203). Put those tests inside the protocol module or define an explicit test seam.
   - Task 2 adds a Rust struct field but does not list or stage `session.rs`; its existing `Diagnostic` literal at [session.rs:811](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bin/strata_kernel_service/session.rs:811) will fail compilation.
   - Task 3 says to sweep every old assertion at [plan:502](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:502), but stages only two files at [plan:510](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:510); the real assertion is in [local_service_recovery.rs:915](/Users/toddhebebrand/Strata/crates/strata-kernel/tests/local_service_recovery.rs:915).
   - Task 5 omits the exact profile-shape acceptance at [full_key_free_acceptance.rs:1943](/Users/toddhebebrand/Strata/crates/strata-kernel/tests/full_key_free_acceptance.rs:1943) and the cross-package request builder at [tasks.test.ts:268](/Users/toddhebebrand/Strata/packages/live-compare/tests/tasks.test.ts:268). Its commit command also omits the Rust tests it says to modify at [plan:794](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:794).

7. **Major — Task 8’s observations do not prove its rollback/health/rehydration claims.** The plan claims that a later successful publication proves rollback, fingerprint equality, and healthy rehydration at [plan:1049](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:1049). A poisoned host can respawn and fully rehydrate on the next request at [persistent.rs:263](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bridge/persistent.rs:263), so later publication does not distinguish same-worker recovery from respawn/fallback. Compare the combined worker-start counter—whose semantics are defined at [kernel.rs:604](/Users/toddhebebrand/Strata/crates/strata-kernel/src/kernel.rs:604)—across both the failed and follow-up requests, and assert the intended rehydration count. The `ps` check may be skipped at [plan:1054](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:1054), so it is not a deterministic key-free cleanup gate.

8. **Major — seed-green and startup-audit sequencing are wrong.** The governing spec says a manifest’s suite is run against generation zero at [item-b-design.md:89](/Users/toddhebebrand/Strata/docs/superpowers/specs/2026-07-31-item-b-design.md:89), but Task 6 invokes baseline validation only for behavioral mode at [plan:838](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:838). A supplied tsc-only manifest must also receive its tsc baseline gate; only the no-manifest default stays unchanged. Moreover, `ServiceSession::open` currently appends `service_started`/`service_recovered` at [session.rs:151](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bin/strata_kernel_service/session.rs:151), before Task 6’s proposed validation. A red daemon would therefore audit that it started and then refuse to bind. Move start/recovery finalization after seed-green. Audit additions must also remain backward-compatible because historical lines are strictly deserialized at [audit.rs:291](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bin/strata_kernel_service/audit.rs:291).

9. **Major — the manifest/reader plan lacks canonical-path containment.** Task 4 specifies lexical module-path validation plus existence/hash checks at [plan:550](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:550). The lexical validator does not resolve symlinks at [protocol.rs:1170](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bin/strata_kernel_service/protocol.rs:1170), while the existing worker explicitly performs `realpath` containment checks at [candidate.ts:449](/Users/toddhebebrand/Strata/packages/kernel-bridge/src/candidate.ts:449). Without the same check in Rust, a registered fixture symlink can escape the corpus and the reader becomes a bounded general file reader. Canonicalize root and fixture, enforce containment before digesting, and retain that verified identity for reads.

10. **Minor — Task 10 should split disclosure implementation from closure, and its declared field types contradict its own serialization test.** The plan says all new fields are optional/defaulted, but three counters are plain `u64` at [plan:1182](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:1182), while its test expects the fields to be absent and byte-identical at [plan:1216](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:1216). Make behavioral-only fields optional/omitted in tsc-only records. Land concurrency-sensitive queue/rehydration metrics green first, then gate wiring and closing records as a separate commit.

## Ten numbered answers

1. **Semantic/operational partition:** Not sound as drafted. Unknown future validate-stage codes correctly default operational. `moduleOutsideSourceRoot`, `invalidBehavioralFixtures`, `invalidValidationProfile`, and `candidateFinalizeFailed` are operational, as shown by [candidate.ts:402](/Users/toddhebebrand/Strata/packages/kernel-bridge/src/candidate.ts:402). Rename-target-not-found is a semantic rejection and should cancel as `ValidationFailed`. The problem is that `mutationFailed` also catches unexpected store/invariant errors; introduce a distinct semantic mutation-rejection code or type.

2. **Claim-intact operational failures:** A claim can strand forever. Quiet daemons generate no ticks, and client-driven `advance` merely returns `claimed` without expiration or execution. `cancel_change_set` is available only to the owning client and abandons rather than retries the work. The plan needs an explicit requeue/release-or-cancel contract. If the response remains `retryable: true`, immediate atomic requeue is the coherent choice.

3. **Deadline plumbing:** Per-kind selection is implementable in `process.rs` because `run` has the full `BridgeRequest` at [process.rs:182](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bridge/process.rs:182), and the router already has separate analyze and candidate call sites at [router.rs:241](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bridge/router.rs:241) and [router.rs:363](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bridge/router.rs:363). The persistent path breaks nesting because queue and sync share the candidate deadline. Worker timeout codes are distinguishable; generic transport deadline errors are not. Add typed phase errors and a distinct queue allowance.

4. **Subprocess cleanup:** `--pool=threads` does use worker threads for Vitest 3.2.4, and the tsc CLI invocation is a single direct Node process at [corpusRun.ts:116](/Users/toddhebebrand/Strata/packages/verify/src/corpusRun.ts:116). It is still insufficient because fixture code may spawn arbitrary descendants. Reject the proposed mechanism and the “divergence” framing; implement process-group cleanup as specified.

5. **Seed-green and timeout lever:** The export-disappearance lever is sound after correcting the path to `../../src/users/greet.ts`; the actual export is at [greet.ts:7](/Users/toddhebebrand/Strata/examples/medium/src/users/greet.ts:7). A namespace import binds the module namespace, not a required `greet` export, so the module still loads after rename, `"greet" in mod` becomes false, and the sleep executes. The plan’s `src/features/greet` path at [plan:1026](/Users/toddhebebrand/Strata/docs/superpowers/plans/2026-08-01-item-b2-behavioral-gate-plan.md:1026) must be corrected.

6. **ValidateBaseline frame:** Keep the new frame. The TypeScript pipeline itself would accept zero intents—the loop at [candidate.ts:92](/Users/toddhebebrand/Strata/packages/kernel-bridge/src/candidate.ts:92) becomes a no-op and validation/export proceeds—but the Rust candidate protocol explicitly rejects empty `orderedIntents` at [bridge/protocol.rs:528](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bridge/protocol.rs:528). Reusing the candidate frame would weaken a valid invariant. Factor shared materialize/validate implementation internally without conflating the wire kinds.

7. **Hello `Ready` fields:** Required plus nullable is correct for a lockstep strict protocol. Current consumers are the Rust `Ready {}` variant at [service protocol.rs:266](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bin/strata_kernel_service/protocol.rs:266), TS strict schema at [live-compare protocol.ts:327](/Users/toddhebebrand/Strata/packages/live-compare/src/protocol.ts:327), hand-built client frames at [client.test.ts:81](/Users/toddhebebrand/Strata/packages/live-compare/tests/client.test.ts:81), and accepted/rejected golden fixtures at [accepted.json:4](/Users/toddhebebrand/Strata/packages/live-compare/tests/fixtures/protocol-v1/accepted.json:4). The stdout readiness line is separate; `service.ts` loosely extracts only `socketPath` at [service.ts:63](/Users/toddhebebrand/Strata/packages/live-compare/src/service.ts:63). The plan’s sweep is adequate if it also updates negative ready fixtures so they remain discriminating tests rather than failing merely for missing required fields.

8. **Task granularity and green state:** No. Tasks 1, 2, 3, and 5 have the compile/staging defects listed in Finding 6. Task 6 needs the seed/audit sequencing correction. Tasks 7 and 9 are otherwise reasonable after real corpus-path and containment corrections. Task 8 needs stronger observations. Split Task 10 into metrics implementation and closure/wiring; that preserves independent green commits without adding scope.

9. **Missed blast radius:**

   - **(a)** `bridge_protocol.rs` contains direct `NodeBridgeConfig` construction at [bridge_protocol.rs:91](/Users/toddhebebrand/Strata/crates/strata-kernel/tests/bridge_protocol.rs:91), and `full_key_free_acceptance.rs` pins exact validation-profile JSON keys. Both must be updated or intentionally preserved.
   - **(b)** Rust and TS stdout-readiness harnesses parse JSON loosely, so additive stdout fields do not break them; new assertions are still needed to prove the metadata. Client-wire hello fixtures are strict and must all change.
   - **(c)** The live assertion in `local_service_recovery.rs` is missed by Task 3’s staged files. The occurrence in [runner.ts:25](/Users/toddhebebrand/Strata/packages/live-compare/src/runner.ts:25) is a benchmark failure category, not the fabricated diagnostic, so it should not be blindly renamed.
   - **(d)** Persistent tests pin both mid-request poisoning and queued-before-worker non-poison behavior at [persistent.rs:1143](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bridge/persistent.rs:1143) and [persistent.rs:1164](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bridge/persistent.rs:1164). Deadline refactoring needs explicit preservation tests.
   - **(e)** `MirrorCandidateResponse` is internally pinned by its parser/router only; Task 1’s proposed diagnostics test is appropriate once moved to an accessible test seam.
   - **(f)** `AuditEvent` is strict at [audit.rs:259](/Users/toddhebebrand/Strata/crates/strata-kernel/src/bin/strata_kernel_service/audit.rs:259). Add optional/defaulted fields, test reopening historical audit lines, include `audit.rs` explicitly, and fix start-event sequencing.
   - Additional misses: `packages/live-compare/tests/tasks.test.ts`, fixture symlink containment, and the Task 10 non-optional counters.

10. **Gate sufficiency:** Task 7 is broadly sufficient for verdict parity, real worker diagnostic propagation, generation preservation, and clean publication once the fixture is corrected to the real corpus. Task 8 is not sufficient. It does not discriminate same-worker rollback/fingerprint health from respawn/fallback, does not actually exercise lease re-offer, allows process-cleanup proof to be skipped, and places rehydration counters in the later Task 10. Gate (d) needs observations of worker-start/rehydration/fallback deltas across both requests, deterministic scratch-tree and process-group cleanup, and a real requeue/re-drive assertion.
hook: Stop
